Pseudo-Code:
// <title>Model Coding Quiz</title>
// <header></header>
    // <header></header> is bold, large and black
    // text-align: center
    // "Coding Quiz Challenge"
    // includes a <div></div> 
    // <p></p> display: block and text-aline: center
        // Instructions on how to take the Quiz
        // font-size: 16px with no text-decoration
    // "Start Quiz" <button></button>
        // style -- centered, background: purple
            // color: white
            // beveled edges
            // Add a box shadow
        // Create a init function to begin the quiz 
            // This should getCorrectAnswers() and getIncorrectAnswers()
    // <body></body>
        // create cards with correlating ids or class tags
            // Create an array for the questions, correct answers and incorrect answers 
        // Create <h2></h2> for all questions
            // they are styled in a larger font, bold
            // update margin -- not in the center but off to the right slightly
        // Create <button></button> for all answers 
            // same styling as the "Start Quiz" <button></button>
            // Create addEventListener("click", function()); and add to all buttons
            // should also include "key" addEventListener
        // Create a View Highscore <button></button>
            // Top left corner of the page
            // create function to store all scores in local storage
            // create a <form></form> to allow the user to input their initials
                // includes a <button>"Submit"</button> 
                    // same styling as all other <button></button>
        // Create a startTimer function 
            // the timer should start at 115 seconds
            // timerCount--; countdown by 1 second decrements
            // need a function to decrement 10 seconds off of the timer for each incorrectAnswer
                // an else if loop might need to be added
            

                